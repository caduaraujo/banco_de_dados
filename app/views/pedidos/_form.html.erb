<%= form_with(model: pedido, local: true) do |form| %>
  <% if pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
        <% pedido.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prato_id %>
    <%= form.text_field :prato_id %>
  </div>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id %>
  </div>

  <div class="field">
    <%= form.label :forma_pagamento %>
    <%= form.check_box :forma_pagamento %>
  </div>

  <div class="field">
    <%= form.label :troco %>
    <%= form.check_box :troco %>
  </div>

  <div class="field">
    <%= form.label :entregador_id %>
    <%= form.text_field :entregador_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
